---
- name: Configure development environment
  hosts: environment
  roles:
    - { role: git, tags: ['git'] }
    - { role: zsh, tags: ['zsh'] }
    - { role: asdf, tags: ['asdf'] }
    - { role: node, tags: ['node'] }
