return {
  'tpope/vim-sleuth',
  config = function()
    -- No additional configuration is needed for vim-sleuth
    -- vim-sleuth automatically adjusts 'shiftwidth' and 'expandtab' based on the current file
  end,
}
