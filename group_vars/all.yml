# Ansible
ansible_python_interpreter: "auto_silent"

# Paths
home: "{{ lookup('env','HOME') }}"
dot_files: "{{ lookup('env','PWD') }}"

# Git
git:
  name: ""
  email: ""
  gpg: ""

asdf:
  version: "v0.8.1"
  shell: "bash"
